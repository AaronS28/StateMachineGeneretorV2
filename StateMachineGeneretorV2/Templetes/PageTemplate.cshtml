using @(Model.SolutionName).Library.Support;
using System.Collections.Generic;

namespace @(Model.SolutionName).Library.Navigation.Pages
{
    public class @(Model.ClassName) : BasePage
    {
        
        @(Model.Constructors)

        @(Model.Properties)

        @(Model.Methods)
    }
}
